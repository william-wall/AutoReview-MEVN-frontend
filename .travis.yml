dist: trusty
language: node_js
node_js:
- "stable"
cache:
   directories:
   - node_modules
before_script:
- npm install cypress --save-dev
- npm run build
- npm run start &
script:
- npx cypress run
